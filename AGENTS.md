# 人類使用說明書 — 專案工作規則

2026-09-07 使用者已確認介面方向，並要求採用適合 AI 開發的技術。現行選型為 React 19 + TypeScript + Vite，取代啟動包原本的 Vue；里程碑範圍與公開發布限制照舊。

## 合作方式
使用繁體中文回覆。使用者負責產品方向，程式碼、設定、測試與文件由 AI 處理。
先 /plan，在同一個 thread 分 3 個里程碑實作；每一階段結束提供可檢查的成果。
規劃模式中不實作。使用者切換／批准實作後，才寫程式、執行測試。
既定需求不重問；細節以合理假設處理，只有付費、公開發布、帳號授權等實質影響事項才先確認。

## 先讀
依序讀 README.md、docs/PRD.md、core/quiz.v1.json、docs/ACCEPTANCE.md、docs/DEPLOY.md。
本包已有原創題庫、娛樂分類核心和核心測試，沒有完成的 React UI 或公開網站。
保留現有檔案；初始化 create-vite 時不要用 force 覆寫資料夾。可在暫存目錄 scaffold 再合併。

## 產品限制
8 題、每題 3 選項、6 種結果；手機優先、繁中、免登入。
先完整實作「開始 → 作答 → 結果 → 儲存圖片／分享 → 重測」。
不做朋友互評、會員、付款、LLM API、資料庫、廣告、追蹤分析。
不得捏造真實用戶數、分享數、心理準確率、科學背書、人格優劣或診斷。
不要讓計分、題目與 UI 混在單一大元件。

## 技術及品質
使用 React 19、TypeScript、Vite、React Router HashRouter、CSS variables；npm 單一 lockfile。
第一版用 hook 管理狀態即可，不為了技術清單加入 Redux 或後端。
以 core/score.mjs 與測試為行為參考；前端移植成嚴格型別的純函式。
開發前確認目前穩定套件與 Node 版本要求；不可直接沿用舊記憶，也不可強裝不相容版本。
鎖定實際安裝的版本並提交 lockfile。遵循環境中的設計與前端技能，先完成視覺方向再寫 UI。
樣式要有完整首頁、作答、結果、分享和手機版設計；不是只做一張 hero。
鍵盤可操作、焦點可見、原生 radio/fieldset、觸控目標充足、尊重 reduced motion。
每階段執行對應測試；最終必須實際打開瀏覽器走流程與檢查畫面，不只跑 build。

## 資料及分享
答案只存此瀏覽器的 localStorage，帶 quizVersion，損壞或寫入失敗時可安全降級。
URL 只放版本和結果類型，不放原始答案、姓名或其他個資。
共用結果頁是角色模板，不假裝已驗證分享者身份。
分享取消不顯示成失敗；不支援原生分享就提供複製連結與圖片儲存。
第一版不承諾社群平台會顯示各角色專屬預覽縮圖。

## GitHub 及公開發布
2026-09-07 使用者已授權建立 mikeggyy/human-manual 公開 repository、上傳目前試玩版並啟用 GitHub Pages。可執行此發布及必要驗證；其他 repository 或可見度變更仍須另行授權。
先在本機準備可部署程式、CI/CD 範本與說明。需要外部動作時先確認目標帳號、repo、可見度與發布授權。
不要讀取或提交金鑰、.env、個人資料、node_modules、測試暫存檔。
不得 force push、清理使用者既有改動或替使用者選擇開源授權。
公開程式碼不等於自動授予任何開源 license；授權由使用者決定。

## 交付標準
完成且驗證的才列為完成；本機通過、CI 通過、公開部署成功要分開說。
未執行的測試、實機檢查、部署都標為未驗證，不能填成成功。
保留 docs/IMPLEMENTATION-PLAN.md，逐項記錄狀態、假設、測試與剩餘事項。
